# 🤖 Настройка Telegram Бота для GameHub

## Шаг 1: Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите название бота (например: "GameHub Marketplace")
4. Введите username бота (например: "YourGameHubBot")
5. BotFather выдаст вам **Bot Token** - сохраните его!

## Шаг 2: Настройка переменных окружения

Добавьте токен бота в файл `/app/backend/.env`:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## Шаг 3: Запуск бота

### Вариант 1: Запуск в фоне (рекомендуется)

```bash
cd /app/backend
nohup python telegram_bot.py > telegram_bot.log 2>&1 &
```

### Вариант 2: Запуск в отдельном терминале

```bash
cd /app/backend
python telegram_bot.py
```

### Вариант 3: Добавить в supervisord

Создайте файл `/etc/supervisor/conf.d/telegram_bot.conf`:

```ini
[program:telegram_bot]
command=python /app/backend/telegram_bot.py
directory=/app/backend
autostart=true
autorestart=true
stderr_logfile=/var/log/telegram_bot.err.log
stdout_logfile=/var/log/telegram_bot.out.log
user=root
```

Затем перезапустите supervisor:

```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start telegram_bot
```

## Шаг 4: Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Вы должны получить приветственное сообщение

## Функции бота

### Для новых пользователей:
- Отправьте: `Имя Фамилия | email@example.com`
- Бот зарегистрирует вас и привяжет Telegram аккаунт

### Для существующих пользователей:
1. Войдите на сайт
2. Перейдите в профиль
3. Нажмите "Привязать Telegram"
4. Получите код
5. Отправьте код боту
6. Аккаунт привязан!

### Вход через Telegram:
- На странице авторизации нажмите "Войти через Telegram"
- Следуйте инструкциям
- Автоматический вход в аккаунт

## Команды бота

- `/start` - Начать работу с ботом
- `/register` - Инструкция по регистрации
- `/help` - Показать справку
- Отправка 8-значного кода - Привязка аккаунта
- Отправка `Имя | Email` - Регистрация

## Безопасность

- Коды действительны 10 минут
- Каждый код можно использовать только один раз
- Telegram ID уникален для каждого пользователя
- Безопасная привязка через JWT токены

## Troubleshooting

### Бот не отвечает:
```bash
# Проверьте логи
tail -f /var/log/telegram_bot.out.log
tail -f /var/log/telegram_bot.err.log

# Проверьте процесс
ps aux | grep telegram_bot

# Перезапустите бота
sudo supervisorctl restart telegram_bot
```

### Ошибка "Token not set":
- Убедитесь, что `TELEGRAM_BOT_TOKEN` добавлен в `/app/backend/.env`
- Перезапустите бота

### Ошибка подключения к MongoDB:
- Убедитесь, что MongoDB запущен
- Проверьте `MONGO_URL` в переменных окружения

## Обновление бота

Замените `@YourGameHubBot` на имя вашего бота в следующих файлах:
- `/app/frontend/src/pages/AuthPage.js` (строка с `@YourGameHubBot`)
- `/app/frontend/src/pages/ProfilePage.js` (строка с `@YourGameHubBot`)
- `/app/backend/telegram_bot.py` (если нужно изменить URL)

## Архитектура

```
User -> Telegram Bot -> MongoDB (codes) -> FastAPI Backend -> Frontend
                                    ↓
                            JWT Token Generation
```

Все готово! 🚀
